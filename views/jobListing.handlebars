{{#each job as |jobs|}}
<div class="card">
    <div class="card-header">
        <p>Posted by {{jobs.customerID}}</p>
    </div>
        <p class="card-body">{{jobs.job_description}}</p>
        <p class="card-body">{{jobs.customer.first_name}}</p>
</div>
{{/each}}