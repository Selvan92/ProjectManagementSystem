{{!-- <div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New job:</h3>

    <form class="form new-project-form">
      <div class="form-group">
        <label for="job-description">Job description:</label>
        <input class="form-input" type="text" id="job-description" name="job-description" />
      </div>
      <div class="form-group">
        <label for="customer-id">Customer:</label>
        <input class="form-input" type="number" id="customer-id" name="customer-id" />
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>

  {{#if jobs.length}}
  <div class="col-md-6 project-list">
    <h3>Current jobs:</h3>

    {{#each jobs as |jobs|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/project/{{jobs.jobs_ID}}">{{jobs.description}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{jobs.jobs_ID}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/profile.js"></script> --}}